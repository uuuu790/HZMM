# 介面縮放（UI Zoom）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 讓使用者用設定頁滑桿與 `Ctrl +/−/0` 縮放整個介面（字+UI+圖示），設定持久化、開 app 自動套用。

**Architecture:** Electron `webFrame.setZoomFactor` 縮放整個 renderer（preload 暴露 `ui.setZoom/getZoom`）；main 在 `did-finish-load` 套用 configStore 的 `uiZoom`；App.jsx 持有 `uiZoom` state + 快捷鍵，SettingsTab 提供滑桿。`clampZoom`/`stepZoom` 純函式走 TDD，其餘整合/UI 以 dev 手動驗證。

**Tech Stack:** Electron（main `webContents` + preload `webFrame`）、React 18、Vitest（`node` 環境）。

## Global Constraints

- 縮放範圍 **0.5–2.0**（50%–200%），step **0.1**，預設 **1.0**；一律經 `clampZoom` 夾住。
- **不做**：字型選擇、UI 密度、per-element 字級。
- **向後相容**：`uiZoom` 預設 1.0 → 現狀不變；縮放是 renderer 視覺層，不影響任何 layout/業務邏輯。
- 滑桿等 UI 用主題變數 `var(--accent-500)`，不寫死。
- Commit style：Conventional Commits。

---

## File Structure

| 檔案 | 職責 | 變更 |
|---|---|---|
| `src/renderer/src/utils/zoom.js` | **新** 純邏輯 | `clampZoom` / `stepZoom` + 常數 |
| `src/preload/index.js` | IPC bridge | 加 `webFrame` import；暴露 `ui.setZoom` / `ui.getZoom` |
| `src/main/index.js` | window 建立 | `did-finish-load` 套用 saved `uiZoom` |
| `src/renderer/src/App.jsx` | 設定狀態 + 快捷鍵 | `uiZoom` state、`handleSetUiZoom`、init 讀取、`Ctrl +/−/0`、傳 prop |
| `src/renderer/src/components/tabs/SettingsTab.jsx` | 設定 UI | 外觀區「介面縮放」滑桿 row |
| `src/renderer/src/constants/i18n/*.js` (×7) | 字串 | `uiZoom` / `uiZoomDesc` / `uiZoomReset` |
| `tests/renderer/zoom.test.js` | **新** | `clampZoom` / `stepZoom` |

---

## Task 1: `clampZoom` / `stepZoom` 純函式

**Files:**
- Create: `src/renderer/src/utils/zoom.js`
- Test: `tests/renderer/zoom.test.js`

**Interfaces:**
- Produces: `clampZoom(z) → number`（夾在 0.5–2.0，非有限值回 1）；`stepZoom(z, delta) → number`（加 delta、四捨五入到 1 位小數、再 clamp）；常數 `ZOOM_MIN=0.5`、`ZOOM_MAX=2.0`、`ZOOM_STEP=0.1`。

- [ ] **Step 1: 寫失敗測試**

建立 `tests/renderer/zoom.test.js`：

```js
import { describe, it, expect } from 'vitest'
import { clampZoom, stepZoom, ZOOM_MIN, ZOOM_MAX } from '../../src/renderer/src/utils/zoom.js'

describe('clampZoom', () => {
  it('clamps below/above the range', () => {
    expect(clampZoom(0.3)).toBe(ZOOM_MIN)
    expect(clampZoom(5)).toBe(ZOOM_MAX)
  })
  it('passes through an in-range value', () => {
    expect(clampZoom(1.3)).toBe(1.3)
  })
  it('returns 1 for non-finite / garbage input', () => {
    expect(clampZoom(NaN)).toBe(1)
    expect(clampZoom('x')).toBe(1)
    expect(clampZoom(undefined)).toBe(1)
  })
})

describe('stepZoom', () => {
  it('steps by delta then clamps', () => {
    expect(stepZoom(1, 0.1)).toBe(1.1)
    expect(stepZoom(1, -0.1)).toBe(0.9)
    expect(stepZoom(ZOOM_MAX, 0.1)).toBe(ZOOM_MAX)
    expect(stepZoom(ZOOM_MIN, -0.1)).toBe(ZOOM_MIN)
  })
  it('avoids floating-point drift', () => {
    expect(stepZoom(1.1, 0.1)).toBe(1.2)
  })
})
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `npx vitest run tests/renderer/zoom.test.js`
Expected: FAIL — 模組不存在。

- [ ] **Step 3: 實作**

建立 `src/renderer/src/utils/zoom.js`：

```js
// Whole-UI zoom bounds. webFrame.setZoomFactor takes a multiplier (1 = 100%).
export const ZOOM_MIN = 0.5
export const ZOOM_MAX = 2.0
export const ZOOM_STEP = 0.1

export function clampZoom(z) {
  const n = Number(z)
  if (!Number.isFinite(n)) return 1
  return Math.max(ZOOM_MIN, Math.min(ZOOM_MAX, n))
}

// Add `delta`, round to one decimal (kills 0.1+0.2 float drift), then clamp.
export function stepZoom(z, delta) {
  return clampZoom(Math.round((clampZoom(z) + delta) * 10) / 10)
}
```

- [ ] **Step 4: 跑測試確認通過**

Run: `npx vitest run tests/renderer/zoom.test.js`
Expected: PASS（全部）。

- [ ] **Step 5: Commit**

```bash
git add src/renderer/src/utils/zoom.js tests/renderer/zoom.test.js
git commit -m "feat: add clampZoom/stepZoom helpers for UI zoom"
```

---

## Task 2: preload `ui` API + main 啟動套用

**Files:**
- Modify: `src/preload/index.js`（line 1 import；contextBridge `api` 物件加 `ui` 區塊）
- Modify: `src/main/index.js`（mainWindow 建立後，約 line 91–195）

**Interfaces:**
- Produces: `window.api.ui.setZoom(factor)`、`window.api.ui.getZoom()`。

無單元測試（Electron/IPC 層）— 以 lint + Task 4 完成後 dev 手動驗證為準。

- [ ] **Step 1: preload 加 webFrame + ui API**

在 `src/preload/index.js` 第 1 行加入 `webFrame`：

```js
const { contextBridge, ipcRenderer, webUtils, webFrame } = require('electron')
```

在 `contextBridge.exposeInMainWorld('api', { … })` 物件內（與其他區塊並列，例如放在 `mods` 區塊之後）新增：

```js
  // --- 介面縮放 ---
  ui: {
    setZoom: (factor) => webFrame.setZoomFactor(factor),
    getZoom: () => webFrame.getZoomFactor(),
  },
```

- [ ] **Step 2: main 啟動套用 saved zoom**

在 `src/main/index.js` 確認頂部已能取得 configStore（若無，加入 `import configStore from './services/config-store.js'`）。在 `mainWindow = new BrowserWindow({…})` 之後、現有 `mainWindow.webContents.on(...)` 監聽附近，加入：

```js
  // Apply the saved UI zoom once the renderer has loaded (avoids a flash).
  mainWindow.webContents.on('did-finish-load', () => {
    const z = Number(configStore.get('uiZoom', 1))
    if (Number.isFinite(z) && z > 0) mainWindow.webContents.setZoomFactor(z)
  })
```

- [ ] **Step 3: Lint**

Run: `npm run lint`
Expected: exit 0。

- [ ] **Step 4: Commit**

```bash
git add src/preload/index.js src/main/index.js
git commit -m "feat: expose webFrame zoom bridge and apply saved zoom on load"
```

---

## Task 3: App.jsx 縮放 state + 快捷鍵

**Files:**
- Modify: `src/renderer/src/App.jsx`（state 約 line 53；handler 約 line 106;init 約 line 245-257;新增 keydown effect;SettingsTab props 約 line 479-484）

**Interfaces:**
- Consumes: `clampZoom`/`stepZoom`/`ZOOM_STEP`（Task 1）、`window.api.ui.setZoom`（Task 2）。
- Produces: props `uiZoom: number`、`handleSetUiZoom(factor): void` 傳給 SettingsTab（Task 4 消費）。

無單元測試（App 整合）— lint + dev 手動驗證。

- [ ] **Step 1: import 純函式**

在 `App.jsx` 既有 import 區加入：

```js
import { clampZoom, stepZoom, ZOOM_STEP } from './utils/zoom';
```

- [ ] **Step 2: 加 state**

在 `const [minimizeToTray, setMinimizeToTray] = useState(true);`（約 line 53）附近加：

```js
  const [uiZoom, setUiZoom] = useState(1);
```

- [ ] **Step 3: 加 handler**

在 `handleSetMinimizeToTray`（約 line 106）附近加（沿用 persistSetting 模式）：

```js
  const handleSetUiZoom = useCallback((factor) => {
    const z = clampZoom(factor);
    setUiZoom(z);
    window.api?.ui?.setZoom?.(z);
    persistSetting('uiZoom', z);
  }, [persistSetting]);
```

- [ ] **Step 4: init 讀取（只同步 state；main 已套用實際縮放）**

在啟動的 `Promise.all([...])`（約 line 245-257）陣列內加一行：

```js
        window.api.settings.get('uiZoom', 1).then(v => setUiZoom(clampZoom(v))),
```

- [ ] **Step 5: 快捷鍵 keydown effect**

在 App 元件內新增一個 effect（放在其他 useEffect 附近）：

```js
  useEffect(() => {
    const onKey = (e) => {
      if (!e.ctrlKey || e.metaKey || e.altKey) return;
      if (e.key === '=' || e.key === '+') { e.preventDefault(); handleSetUiZoom(stepZoom(uiZoom, ZOOM_STEP)); }
      else if (e.key === '-' || e.key === '_') { e.preventDefault(); handleSetUiZoom(stepZoom(uiZoom, -ZOOM_STEP)); }
      else if (e.key === '0') { e.preventDefault(); handleSetUiZoom(1); }
    };
    window.addEventListener('keydown', onKey);
    return () => window.removeEventListener('keydown', onKey);
  }, [uiZoom, handleSetUiZoom]);
```

- [ ] **Step 6: 傳 props 給 SettingsTab**

在 `<SettingsTab … />`（約 line 461-485）的 props 內，緊接 `handleSetSkipInstallPreview={handleSetSkipInstallPreview}` 之後加：

```jsx
              uiZoom={uiZoom}
              handleSetUiZoom={handleSetUiZoom}
```

- [ ] **Step 7: Lint**

Run: `npm run lint`
Expected: exit 0。

- [ ] **Step 8: Commit**

```bash
git add src/renderer/src/App.jsx
git commit -m "feat: UI zoom state, handler, startup sync, and Ctrl +/- shortcuts"
```

---

## Task 4: SettingsTab 滑桿 UI + i18n

**Files:**
- Modify: `src/renderer/src/components/tabs/SettingsTab.jsx`（props 解構；外觀區加滑桿 row）
- Modify: `src/renderer/src/constants/i18n/*.js`（×7）

**Interfaces:**
- Consumes: props `uiZoom`、`handleSetUiZoom`（Task 3）。

無單元測試（UI）— lint + i18n-completeness + dev 手動驗證。

- [ ] **Step 1: i18n ×7**

在每個 `src/renderer/src/constants/i18n/<lang>.js` 的 `appearanceDesc` 那行**之後**加 3 個鍵（值依語言）。zh-TW：

```js
  "uiZoom": "介面縮放",
  "uiZoomDesc": "縮放整個介面的字體與元件",
  "uiZoomReset": "重置",
```

各語言對應值：
- en：`"Interface zoom"` / `"Scale the whole UI — text and components"` / `"Reset"`
- ja：`"画面ズーム"` / `"UI全体（文字と要素）を拡大縮小"` / `"リセット"`
- ko：`"화면 배율"` / `"UI 전체(글자와 요소) 크기 조절"` / `"초기화"`
- ru：`"Масштаб интерфейса"` / `"Масштабирует весь интерфейс — текст и элементы"` / `"Сброс"`
- de：`"Oberflächen-Zoom"` / `"Skaliert die ganze Oberfläche — Text und Elemente"` / `"Zurücksetzen"`
- fr：`"Zoom de l'interface"` / `"Met à l'échelle toute l'interface — texte et éléments"` / `"Réinitialiser"`

- [ ] **Step 2: 滑桿 row UI**

在 `SettingsTab.jsx` 的元件 props 解構中加入 `uiZoom`、`handleSetUiZoom`（與 `minimizeToTray` 等並列）。

在「外觀」主題切換卡片**之後**（minimizeToTray row 之前），插入「介面縮放」row：

```jsx
        <GlassCard isPill={false} className="flex items-center gap-4 px-5 py-4 md:px-6 md:py-5">
          <div className="flex flex-col flex-1 min-w-0">
            <h4 className="text-sm md:text-base font-bold text-slate-800 dark:text-slate-100 truncate leading-tight transition-colors duration-700">{t.uiZoom}</h4>
            <p className="text-[11px] md:text-xs text-slate-500 dark:text-slate-400 truncate font-medium transition-colors duration-700">{t.uiZoomDesc}</p>
          </div>
          <div className="flex items-center gap-3 shrink-0">
            <input
              type="range" min="0.5" max="2" step="0.1" value={uiZoom}
              onChange={(e) => handleSetUiZoom(parseFloat(e.target.value))}
              className="w-28 sm:w-40 accent-[var(--accent-500)] cursor-pointer"
            />
            <span className="w-12 text-right text-sm font-bold font-mono text-slate-700 dark:text-slate-200 tabular-nums">{Math.round(uiZoom * 100)}%</span>
            <button
              onClick={() => handleSetUiZoom(1)}
              className="px-3 py-1.5 text-xs font-bold rounded-full text-slate-500 dark:text-slate-400 hover:bg-slate-100 dark:hover:bg-slate-800 transition-colors"
            >
              {t.uiZoomReset}
            </button>
          </div>
        </GlassCard>
```

（若該 `GlassCard` 需要 import，`SettingsTab.jsx` 既有同款卡片已 import，沿用即可。實際包裹結構請對齊既有 minimizeToTray row 的外層容器。）

- [ ] **Step 3: i18n 完整性 + lint**

Run: `npx vitest run tests/renderer/i18n-completeness.test.js && npm run lint`
Expected: i18n-completeness PASS（7 語言鍵一致）+ lint exit 0。

- [ ] **Step 4: 全套件**

Run: `npm test`
Expected: 全部通過（新增 zoom 測試 + i18n；無破壞）。

- [ ] **Step 5: 手動驗證（dev）**

1. 設定頁外觀區出現「介面縮放」滑桿；拖動即時縮放整個介面、百分比同步更新。
2. 「重置」鈕回 100%。
3. `Ctrl +` / `Ctrl −` / `Ctrl 0` 放大/縮小/重置。
4. 重開 app：自動套用上次的縮放，且滑桿百分比正確。

- [ ] **Step 6: Commit**

```bash
git add src/renderer/src/components/tabs/SettingsTab.jsx src/renderer/src/constants/i18n/
git commit -m "feat: UI zoom slider in settings + i18n"
```

---

## Self-Review

- **Spec coverage：** §1 機制（webFrame）→ Task 2；§2 範圍/clamp → Task 1 + Global Constraints；§3 持久化+啟動套用 → Task 2（main）+ Task 3（init/handler persist）；§4 設定 UI 滑桿 → Task 4；§5 快捷鍵 → Task 3；§6 i18n → Task 4；§7 元件分解 → File Structure；§8 測試 → Task 1 vitest + Task 4 手動。全覆蓋。
- **Placeholder scan：** 無 TBD/TODO；每個改 code 步驟附完整程式碼。Task 2 Step 2「若無則 import configStore」與 Task 4 Step 2「對齊既有 row 外層容器」是對既有結構的具體指示，非佔位。
- **Type consistency：** `clampZoom(z)→number`、`stepZoom(z,delta)→number`、`ZOOM_STEP`（Task 1）一致；`handleSetUiZoom(factor)` + `uiZoom`（Task 3 產生）= Task 4 消費的 props；`window.api.ui.setZoom`（Task 2）= Task 3 呼叫；configStore key `uiZoom`（Task 2 main / Task 3 persist）一致。
